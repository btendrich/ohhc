<%= bootstrap_form_for( @session_spot, layout: :horizontal ) do |form| %>
  <% if session_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session_spot.errors.count, "error") %> prohibited this session_spot from being saved:</h2>

      <ul>
      <% session_spot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
                <%= form.number_field :child_id, id: :session_spot_child_id %>
            </div>

    <div class="field">
                <%= form.number_field :hosting_session_id, id: :session_spot_hosting_session_id %>
            </div>

    <div class="field">
                <%= form.number_field :spot_status_id, id: :session_spot_spot_status_id %>
            </div>

    <div class="field">
                <%= form.number_field :scholarship, id: :session_spot_scholarship %>
            </div>

    <div class="field">
                <%= form.number_field :row_order, id: :session_spot_row_order %>
            </div>

    <div class="field">
                <%= form.text_area :public_notes, id: :session_spot_public_notes %>
            </div>

    <div class="field">
                <%= form.text_area :private_notes, id: :session_spot_private_notes %>
            </div>

    <div class="btn">
        <%= form.submit class: 'btn btn-sm btn-outline dim btn-success' do %>
            <i class="fa fa-chevron-left"></i> Submit
        <% end %>
    </div>
<% end %>

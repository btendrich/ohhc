<%= bootstrap_form_for( @child_photo, layout: :horizontal ) do |form| %>
  <% if child_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child_photo.errors.count, "error") %> prohibited this child_photo from being saved:</h2>

      <ul>
      <% child_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
                <%= form.number_field :child_id, id: :child_photo_child_id %>
            </div>

    <div class="field">
                <%= form.text_field :description, id: :child_photo_description %>
            </div>

    <div class="field">
                <%= form.text_field :key, id: :child_photo_key %>
            </div>

    <div class="field">
                <%= form.number_field :row_order, id: :child_photo_row_order %>
            </div>

    <div class="btn">
        <%= form.submit class: 'btn btn-sm btn-outline dim btn-success' do %>
            <i class="fa fa-chevron-left"></i> Submit
        <% end %>
    </div>
<% end %>

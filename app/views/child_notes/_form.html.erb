<%= bootstrap_form_for( @child_note, layout: :horizontal ) do |form| %>
  <% if child_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child_note.errors.count, "error") %> prohibited this child_note from being saved:</h2>

      <ul>
      <% child_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
                <%= form.number_field :child_id, id: :child_note_child_id %>
            </div>

    <div class="field">
                <%= form.text_field :title, id: :child_note_title %>
            </div>

    <div class="field">
                <%= form.text_area :text, id: :child_note_text %>
            </div>

    <div class="btn">
        <%= form.submit class: 'btn btn-sm btn-outline dim btn-success' do %>
            <i class="fa fa-chevron-left"></i> Submit
        <% end %>
    </div>
<% end %>

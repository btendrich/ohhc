<%= bootstrap_form_for( @hosting_session_spot, layout: :horizontal ) do |form| %>
  <% if hosting_session_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hosting_session_spot.errors.count, "error") %> prohibited this hosting_session_spot from being saved:</h2>

      <ul>
      <% hosting_session_spot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
                <%= form.number_field :status_id, id: :hosting_session_spot_status_id %>
            </div>

    <div class="field">
                <%= form.number_field :hosting_session_id, id: :hosting_session_spot_hosting_session_id %>
            </div>

    <div class="field">
                <%= form.text_area :description, id: :hosting_session_spot_description %>
            </div>

    <div class="field">
                <%= form.number_field :scholarship, id: :hosting_session_spot_scholarship %>
            </div>

    <div class="field">
                <%= form.number_field :family_id, id: :hosting_session_spot_family_id %>
            </div>

    <div class="btn">
        <%= form.submit class: 'btn btn-sm btn-outline dim btn-success' do %>
            <i class="fa fa-chevron-left"></i> Submit
        <% end %>
    </div>
<% end %>
